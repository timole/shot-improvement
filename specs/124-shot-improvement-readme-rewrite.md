# 124 — README rewritten

README.md rewritten from scratch: short description (ice hockey), feature
list, architecture diagram, then how to run. Paths use the home
directory (`~\Documents\Projektit\shot-improvement`), not the user's
full profile path. The old README's Azure resource/runbook notes moved
to `docs/azure.md`; the long per-spec list is dropped (see `specs/`).
